{
  "name": "overleaf-comment-exporter",
  "version": "0.1.0",
  "private": true,
  "description": "Firefox extension to export Overleaf review comments as CSV or XLSX",
  "scripts": {
    "build": "esbuild src/content.ts --bundle --format=iife --outfile=dist/content.js && esbuild src/popup.ts --bundle --format=iife --outfile=dist/popup.js",
    "typecheck": "tsc --noEmit",
    "lint:firefox": "web-ext lint -s .",
    "pack": "web-ext build -s . -a web-ext-artifacts --filename overleaf-comment-exporter-{version}.zip --ignore-files src/** node_modules/** tsconfig.json DIAGNOSTIC.md package-lock.json",
    "package": "npm run build && npm run pack"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.3",
    "web-ext": "^8.9.0"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "jszip": "^3.10.1"
  }
}
